name: Keep Render App Alive

on:
  schedule:
    - cron: '*/40 * * * *'  # Runs every 40 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: curl -s https://lifetag-publicscanner.onrender.com > /dev/null
